from setuptools import setup, find_packages

setup(
    name="akinator_automation_api",
    version="0.1.0",
    description="A simple tool to enable automation of akinator games using an LLM",
    author="",
    packages=find_packages(),
    python_requires=">=3.8",
)
